/** Quantidade de mensagens recentes na memória curta (orquestração — Issue #9). */
export const SHORT_TERM_MESSAGE_LIMIT = 10;

/** Memória curta reduzida quando `riskLevel === high` (modo seguro — Parte 9.2). */
export const SAFE_MODE_RECENT_MESSAGE_LIMIT = 1;
